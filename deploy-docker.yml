---
- name: setup pre-requisites
  hosts: all
  become: yes
  roles:
    - install-modules
    - configure-hosts-file

- name: install docker
  become: yes
  hosts: dockernodes
  roles:
    - docker-installation
